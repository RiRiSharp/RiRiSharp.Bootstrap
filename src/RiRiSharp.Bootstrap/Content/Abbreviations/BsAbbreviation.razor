@inherits RiRiSharp.Bootstrap.BaseComponents.BsChildContentComponent

<abbr @attributes="AdditionalAttributes" title="@(FullName)" class="@(Type.ToBootstrapClass()) @(Classes)" @ref="htmlRef">
    @(ChildContent)
</abbr>


@code {
    [Parameter, EditorRequired] public string FullName { get; set; }
    [Parameter] public BsAbbreviationType Type { get; set; }
}