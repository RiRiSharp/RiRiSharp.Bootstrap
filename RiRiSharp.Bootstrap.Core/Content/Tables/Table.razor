@inherits BootstrapCoreChildContentComponent

<table @attributes="AdditionalAttributes" class="table @(Options.ToBootstrapClass()) @(Responsiveness.ToBootstrapClass()) @(Classes)">
    @(ChildContent)
</table>

@code{
    [Parameter] public TableOptions Options { get; set; }
    [Parameter] public TableResponsiveness Responsiveness { get; set; }
}